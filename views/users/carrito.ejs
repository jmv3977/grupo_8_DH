<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <link rel="stylesheet" href="/stylesheets/stylesFH.css">
    <title> Vitalcer|  Mi Carrito</title>
</head>


<body class="bodyCarrito">
     <!-- header -->
<%- include('partials/header')%>

    <!-- productos carrito -->
    <div class = "carrito-container">
        <h1> Mi carrito </h1>
    </div>

    <div class="px-4 px-lg-0">
    
      <div class="pb-5">
        <div class="container">
          <div class="row">
            <div class="col-lg-12 p-5 bg-white rounded shadow-sm mb-5">
    
              <!-- Shopping cart table -->
              <div class="table-responsive">
                <table class="table"> 
                  <thead>
                    <tr>
                      <th scope="col" class="border-0 bg-light">
                        <div class="p-2 px-3 text-uppercase">Productos</div>
                      </th>
                      <th scope="col" class="border-0 bg-light">
                        <div class="py-2 text-uppercase">Precio</div>
                      </th>
                      <th scope="col" class="border-0 bg-light">
                        <div class="py-2 text-uppercase">Cantidad</div>
                      </th>
                      <th scope="col" class="border-0 bg-light">
                        <div class="py-2 text-uppercase">Eliminar</div>
                      </th>
                    </tr>
                  </thead>
                  <tbody>
                    <% products.forEach(function(product){ %>
                    <tr>
                      <th scope="row" class="border-0">
                        <div class="p-2">
                          <img src="/images/products/<%= product.image %>" alt="imagen de producto" width="70" class="img-fluid rounded shadow-sm">
                          <div class="ml-3 d-inline-block align-middle">
                            <h5 class="mb-0"> <a href="#" class="text-dark d-inline-block align-middle"><%= product.name %></a></h5><span class="text-muted font-weight-normal font-italic d-block"><%= categoria.name%></span>
                          </div>
                        </div>
                      </th>
                      <td class="border-0 align-middle"><strong><%= product.price %></strong></td>
                      <td class="border-0 align-middle"><select class="form-control" id="exampleFormControlSelect1">
                        <option>1</option>
                        <option>2</option>
                        <option>3</option>
                        <option>4</option>
                        <option>5</option>
                      </select></td>
                      <td class="border-0 align-middle"><a href="#" class="text-dark"><i class="fa fa-trash"></i></a></td>
                      <% }) %>  
                    </tr>
                  </tbody>
                </table>
              </div>              
            </div>
          </div>

          <!-- lista a pagar --> 
          <div class="col-lg-6">
              <div class="bg-light rounded-pill px-4 py-3 text-uppercase font-weight-bold">Cantidad a pagar </div>
                <ul class="listaB lista-cant-a-pagar">
                  <li class="listaA d-flex justify-content-between py-3 border-bottom"><strong class="text-muted">Subtotal </strong><strong>$640,00</strong></li>
                  <li class="listaA d-flex justify-content-between py-3 border-bottom"><strong class="text-muted">Envio</strong><strong>$10,00</strong></li>
                  <li class="listaA d-flex justify-content-between py-3 border-bottom"><strong class="text-muted">Descuento</strong><strong>$0,00</strong></li>
                  <li class="listaA d-flex justify-content-between py-3 border-bottom"><strong class="text-muted">Total</strong>
                    <h5 class="font-weight-bold">$650,00</h5>
                  </li>
                </ul><a href="#" class="btn btn-dark rounded-pill py-2 btn-block">Finalizar compra</a>             
            </div>
        </div>
      </div>
    </div>
    
    <!-- seccion productos similares -->
    <div class = "similar-product">
        <h2>Productos similares</h2>
    </div>
    <% products.forEach(function(product){ %>
        <section class = card2>
            <div class="card" style="width: 18rem;">
                <img src="/images/products/<%= product.image %>" class="card-img-top" alt="">
              <div class="card-body">
                <h5 class="card-title"><%= product.name %></h5>
                <p class="card-text"><%= product.price %></p>
                <a href="#" class="btn btn-primary">Agregar al carrito</a>
              </div>
            </div>
        </section>
    <% }) %>
<!-- footer -->
<%- include('partials/footer')%> 

</body>
  
</html>